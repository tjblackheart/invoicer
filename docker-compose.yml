version: "3"
services:
    invoicer:
        build: .
        image: tjblackheart/invoicer:latest
        ports:
            - 3000:3000
        volumes:
            - ./app/var:/app/var
        env_file:
            - ./app/.env
